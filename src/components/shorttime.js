import React from "react";
import '../css/shortTime.css';

function ShortTime ({ tiempoCorto }) {
  return (
    <div className="ShortTime">
      {tiempoCorto}
    </div>
  );
};

export default ShortTime;